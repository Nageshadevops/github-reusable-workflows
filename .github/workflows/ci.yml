name: Continuous Integration - Reusable
on: [workflow_call]
jobs:
  CI_Steps:
    runs-on: [ci-server-ec2]
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4
      - run: echo OK
        name: Code Build
      - run: echo OK
        name: Code Test
      - run: echo OK
        name: Code Review
      - run: echo OK
        name: Release Software
        #